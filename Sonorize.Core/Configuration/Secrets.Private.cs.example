namespace Sonorize.Core.Configuration;

/// <summary>
/// Template for application secrets.
/// To enable Last.fm features:
/// 1. Copy this file.
/// 2. Remove the ".example" extension (rename to Secrets.Private.cs).
/// 3. Paste your API keys below.
/// </summary>
public static partial class Secrets
{
    static partial void InjectLastFmKeys(ref string apiKey, ref string apiSecret)
    {
        // Get these from https://www.last.fm/api/account/create
        apiKey = "YOUR_API_KEY";
        apiSecret = "YOUR_SHARED_SECRET";
    }
}